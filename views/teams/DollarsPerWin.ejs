<% layout('layouts/boilerplate') %>
<div class="container mt-5">
    <div class="card card-bg shadow">
        <div class="card-body">
            <h3 class="card-title mb-4">Team Dollars Spent Per Win Data</h3>
            <% if (dollarsPerWin && dollarsPerWin.length) { %>
                <div class="table-responsive">
                    <table class="table table-dark table-striped table-bordered align-middle">
                        <thead>
                            <tr>
                                <th>Team ID</th>
                                <th>Year</th>
                                <th>Dollars Spent Per Win</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% dollarsPerWin[0][0].forEach(row => { %>
                                <tr>
                                    <td><%= row.team_id %></td>
                                    <td><%= row.year %></td>
                                    <td><%= row.dollarsPerWin %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div class="alert alert-warning">No salary stats available for this team.</div>
            <% } %>
            <a href="/team" class="btn btn-secondary mt-3">Back</a>
        </div>
    </div>
</div>